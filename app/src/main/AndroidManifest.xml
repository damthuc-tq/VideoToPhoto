<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.videotophoto_grabphotosfromvideo">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:requestLegacyExternalStorage="true"
        android:allowBackup="true"
        android:icon="@drawable/icon_app"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VideoToPhotoGrabPhotosFromVideo">
        <activity android:name=".gallery.activity.ViewPhotoActivity"></activity>
        <activity android:name=".setting.SettingActivity" />
        <activity android:name=".WebViewActivity" />
        <activity android:name=".slide_show_maker.SelectImageToSlideActivity" />
        <activity android:name=".select_video.activity.VideoToPhotoActivity" />
        <activity android:name=".select_video.activity.VideoListActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".slide_show_maker.SlideShowMakerActivity" />
        <activity android:name=".gallery.activity.GalleryActivity" />
        <activity android:name=".select_video.activity.SelectVideoActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>